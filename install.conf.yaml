- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.aliases: aliases
    ~/.bashrc: bashrc
    ~/.functions: functions
    ~/.inputrc: inputrc
    ~/.exports: exports
    ~/.prompt: prompt
    
- shell:
  - [git submodule update --init --recursive, Installing submodules]
